ADB WMS Controller

ADB WMS Controller is a desktop automation tool built with Python and Tkinter that uses Android Debug Bridge (ADB) to control Android devices and automate repetitive operations in warehouse management systems (WMS).

The tool can detect connected Android devices, send automated commands, and simulate device interactions such as text input and screen taps. It is designed to help reduce repetitive manual work and improve operational efficiency.